#!/usr/bin/env bash

set -e

# Detect OS and call appropriate script
OS="$(uname -s)"

case "$OS" in
  Darwin)
    exec "$(dirname "$0")/set-theme-macos" "$@"
    ;;
  Linux)
    exec "$(dirname "$0")/set-theme-linux" "$@"
    ;;
  *)
    echo "Unsupported OS: $OS"
    exit 1
    ;;
esac
